version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22 || nvm install 22
        - node -v
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # SSR build: Amplify serves .next via Hosting compute.
    # Do not change to "out" - this app has API routes and cannot be statically exported.
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
